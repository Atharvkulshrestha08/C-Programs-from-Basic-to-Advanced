#include <stdio.h>
void main(){
	int a[20],i,n,sum=0;
	printf("Enter array size\n");
	scanf("%d",&n);
	printf("Enter array elements\n");
	for (i=0;i<n;i++){
		scanf("%d",&a[i]);
		sum = sum + a[i];
	}
	printf("Sum of the array = %d\n",sum);
}
