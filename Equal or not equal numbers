#include <stdio.h>
int main(){
	int num1, num2;
	printf("Enter two numbers= ");
	scanf("%d %d", &num1, &num2);
	if (num1 == num2){
		printf("Both the numbers are equal\n");
	} else {
		printf("Both the numbers are not equal\n");
	}
	return 0;
}
