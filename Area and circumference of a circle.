#include <stdio.h>
#include <math.h>
int main(){
	float r,a,c;
	printf("Enter radius= ");
	scanf("%f",&r);
	a = (3.14*r*r);
	printf("Area of Circle= %.2f",a);
	c=(2.0*3.14*r);
	printf("Circumference of Circle= %.2f",c);
	return 0;
}
